# Event aggregation notes

## What this adds

- aggregated guest comms event contract
- sorting/grouping utilities
- example aggregation set

## Why it matters

The timeline, analytics, and review views all get easier once there’s a common event shape instead of every screen inventing its own weird translation layer.
