# wedding-site-Bolt integration notes

Use this folder to document exactly how a feature should plug into the main DayOf app.

For each feature, capture:
- expected destination files/folders
- required routes/pages/components
- required environment/config variables
- required Supabase tables/functions/policies
- smoke-test expectations after integration
